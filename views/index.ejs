<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.1/css/bootstrap.min.css" integrity="sha384-WskhaSGFgHYWDcbwN70/dfYBj47jz9qbsMId/iRN3ewGhXQFZCSftd1LZCfmhktB" crossorigin="anonymous">
    <title>Ninja Gold Application</title>
    <style>
        .wrapper {
            padding: 10px;
        }
        form {
            display: inline-block;
            text-align: center;
            border: 2px solid black;
            width: 200px;
            height: 200px;
            vertical-align: top;
            padding: 10px;
            margin-bottom: 10px;
        }
        #chat {
            width: 55%;
            height: 200px;
            overflow: scroll;
            overflow-x:hidden;
            overflow-y:visible;
        }
        input {
            text-align: center;
        }

    </style>
    <script type="text/javascript">
        window.onload = function() {
            var element = document.getElementById("chat");
            element.scrollTop = element.scrollHeight;
        }
    </script>
</head>
<body>
    <div class="wrapper">
        <label for="gold">Your Gold</label>
        <input type="text" value="<%= total %>">
        <br>
        <form action="/process/Farm" method="POST">
            <h2>Farm</h2>
            <h4>(earns 10 - 20 gold)</h4>
            <br>
            <input type="submit" name="gold" value="Find Gold!!" class="btn btn-warning">
        </form>
        <form action="/process/Cave" method="POST">
            <h2>Cave</h2>
            <h4>(earns 5 - 10 gold)</h4>
            <br>
            <input type="submit" name="gold" value="Find Gold!!" class="btn btn-warning">
        </form>
        <form action="/process/House" method="POST">
            <h2>House</h2>
            <h4>(earns 2 - 5 gold)</h4>
            <br>
            <input type="submit" name="gold" value="Find Gold" class="btn btn-warning">
        </form>
        <form action="/process/Casino" method="POST">
            <h2>Casino</h2>
            <h4>(earns/takes 0 - 50 gold)</h4>
            <br>
            <input type="submit" name="gold" value="Find Gold!!" class="btn btn-warning">
        </form>
        <div class="activites">
            <p>Activites:</p>
            <div id="chat">
            <% for(var message of messages){ %>
                <% if(message.gold > 0){ %>
                    <p class='text-success'>You earned <%=message.gold%> gold from the <%=message.location%> !</p>
                <% }else{ %>
                    <p class='text-danger'>You lost <%=message.gold%> gold from the <%=message.location%> ....ouch!</p>
                <% } %>
            <% } %>
            </textarea>
        </div>
        <a href="/reset"><button class="btn btn-dark">Play Again</button></a>

    </div>
</body>
</html>
